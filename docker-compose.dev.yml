services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sdlc
      POSTGRES_PASSWORD: sdlc
      POSTGRES_DB: sdlc
    ports:
      - "5433:5432"
    volumes:
      - sdlc_pg_data:/var/lib/postgresql/data

volumes:
  sdlc_pg_data:
